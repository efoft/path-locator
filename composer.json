{
    "name": "efoft/path-locator",
    "type": "library",
    "description": "Helper class to detect site root path, base directory and do some convertions.",
    "require": {
        "php": ">=5.3.0"
    },
    "autoload": {
        "psr-4": {
            "ActiveRecords\\": "src"
        }
    }
}
